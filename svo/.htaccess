# SVO redirects
#
# https://w3id.org/svo redirects to https://scientificvariablesontology.org/
#
# ## Contact
# This space is administered by:
#
# Maria Stoica
# maria.stoica@colorado.edu
# GitHub username: mariutzica

AddType text/turtle .ttl
AddType application/rdf+xml .owl

RewriteEngine on

# Rewrite rule to serve HTML content
RewriteCond %{HTTP_ACCEPT} text/html
RewriteRule ^(.*)$ https://scientificvariablesontology.org/svo/$1/index.html [R=303,L]

# Rewrite rule to serve TTL content -- last match needs to be fixed ...
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^(.*)/(.*)$ https://scientificvariablesontology.org/svo/$1/$2/$2.ttl [R=303,L]

# Rewrite rule to serve RDF/XML content if requested
RewriteCond %{HTTP_ACCEPT} application/rdf\+xml
RewriteRule ^(.*)/(.*)$ https://scientificvariablesontology.org/svo/$1/$2/$2.ttl [R=303,L]
